%h1 Tasks

%table.table.table-hover.table-condensed
  %tr
    %th Project
    %th Tracker

  - @tasks.each do |task|
    %tr
      %td= task.description
      %td= link_to "Tracker Story", '#', target: "_blank"

