%h1 Tasks

%h2 Open
%table.table.table-hover.table-condensed
  %tr
    %th Task
    %th Type of Task
    %th Size
    %th Project

  - @tasks.each do |task|

    %tr
      %td= link_to task.description, task
      %td= task.type_of_task
      %td= task.size
      %td= link_to task.project.name, task.project


.btn= link_to 'New Task', new_task_path

